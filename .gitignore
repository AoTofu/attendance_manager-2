# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# macOS
.DS_Store

# Virtual environments
.venv/
venv/

# Distribution / packaging
build/
dist/
*.spec

# Sensitive runtime secrets
initial_admin_password.txt

# SQLite databases in user data dir are runtime artifacts
# (left unignored here because path is outside repo; nothing to ignore)
